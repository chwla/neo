import {useEffect,useState} from "react";import {api} from "./api.js";
export default function Continuity(){const [bundles,setBundles]=useState([]),[root,setRoot]=useState(""),[detail,setDetail]=useState(null),[error,setError]=useState("");const refresh=()=>api.continuityBundles().then(x=>setBundles(x.bundles||[]));useEffect(()=>{refresh();},[]);const exportBundle=async()=>{try{const b=await api.continuityExport({bundle_type:"workspace",root_entity_type:"workspace",root_entity_id:root});setDetail(await api.continuityReport(b.id));await refresh();}catch(e){setError(e.message)}};return <div className="provider-runtime"><h2>Continuity / Export / Import</h2><p>Portable, redacted project state and reference validation.</p><input aria-label="Workspace ID" value={root} onChange={e=>setRoot(e.target.value)} placeholder="Workspace ID"/><button disabled={!root} onClick={exportBundle}>Export workspace</button>{error&&<p className="neo-error">{error}</p>}<h3>Bundles</h3>{bundles.map(b=><button className="provider-row" key={b.id} onClick={async()=>setDetail(await api.continuityReport(b.id))}>{b.bundle_name} · {b.status}</button>)}{detail&&<section><h3>Continuity report</h3><pre>{JSON.stringify(detail,null,2)}</pre></section>}</div>}
