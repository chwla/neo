"""Hardware-aware local model setup.

Looks at the machine, works out which open-weights models it can actually run, says so
in language someone who has never heard of a GGUF can act on, and installs the one they
pick.
"""
